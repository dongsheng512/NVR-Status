[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = [
    "cli_report",
    "config_store",
    "hikvision_status",
    "main",
    "run_gui",
]
packages = ["ui", "ui.panels", "ui.widgets", "services", "nvr_core"]

[project]
name = "cam"
version = "2.0.0"
description = "海康 NVR 状态巡检与音视频抽检（CLI + GUI）"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
    "PySide6~=6.8.0",
    "rich>=15.0.0",
]

[project.scripts]
nvr-gui = "ui.app:main"

[project.optional-dependencies]
build = [
    "pyinstaller>=6.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
